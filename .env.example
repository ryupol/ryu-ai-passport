# Local defaults for the first Compose slice.
#
# Copy to `.env` for local development. Do not commit `.env`.

OLLAMA_IMAGE=ollama/ollama:latest
OLLAMA_BIND_ADDR=127.0.0.1
OLLAMA_SMOKE_MODEL=llama3.2:1b
